name: Swift
on:
  push:
    branches: [ SP-7327_add_ui_tests ]
  pull_request:
    branches: [ SP-7327_add_ui_tests ]
defaults:
  run:
    working-directory: Example
jobs:
  lint:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: linting
        run: swiftlint lint
  meta-app-tests:
      runs-on: macos-latest
      steps:
        - uses: actions/checkout@v2
        - name: SourcePointMetaApp UI testing -> iPhone 11 (iOS 14.4)
          run: xcodebuild clean test -scheme SourcePointMetaApp -workspace ConsentViewController.xcworkspace -destination 'platform=iOS Simulator,name=iPhone 11,OS=14.5' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGN_ENTITLEMENTS="" CODE_SIGNING_ALLOWED="NO"


